[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mediavocab"
dynamic = ["version"]
description = "Reference vocabulary and pydantic data model for media cataloguing."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [{name = "JarbasAi", email = "jarbasai@mailfence.com"}]
keywords = ["media", "metadata", "taxonomy", "pydantic", "vocabulary"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
    "Typing :: Typed",
    "Topic :: Multimedia",
]
dependencies = [
    "pydantic>=2",
    "ovos-spec-tools>=0.6.0a1",
]

[project.optional-dependencies]
test = ["pytest>=7", "pytest-cov"]

[project.urls]
Homepage = "https://github.com/TigreGotico/mediavocab"
Repository = "https://github.com/TigreGotico/mediavocab"
Issues = "https://github.com/TigreGotico/mediavocab/issues"

[tool.setuptools.dynamic]
version = {attr = "mediavocab.version.__version__"}

[tool.setuptools.packages.find]
include = ["mediavocab*"]
exclude = ["tests*", "examples*", "docs*"]

[tool.setuptools.package-data]
"mediavocab" = ["py.typed"]
"mediavocab.locale" = ["**/*.voc"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
